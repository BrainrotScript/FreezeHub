task.spawn(function()
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet("https://pastefy.app/SRvUTMmT/raw", true))()
    end)
    
    if not success1 then
        warn("Erro no Script 1:", error1)
    end
end)

task.wait(3) -- espera 3 segundos antes do segundo script

task.spawn(function()
    local success2, error2 = pcall(function()
        loadstring(game:HttpGet("https://pastebin.com/raw/7k2gt17G", true))()
    end)
    
    if not success2 then
        warn("Erro no Script 2:", error2)
    end
end)
